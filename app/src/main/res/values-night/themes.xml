<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.TaskManagment" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="colorPrimary">@color/red</item>
        <item name="colorSecondary">@color/red_light</item>

        <item name="backgroundLayout">@color/gray_dark</item>
        <item name="android:textColor">@color/white</item>
        <item name="backgroundCard">@color/black</item>
        <item name="IconColor">@color/white</item>
        <item name="android:statusBarColor">@color/black</item>

    </style>
</resources>